'use strict';

const composejs = require('..');

describe('composejs', () => {
    it('needs tests', (done) => {
        composejs("/Users/ouyangxin/Workspace/CrossPlatform/MiniTemp")
        setTimeout(done, 3000)
    });
});
